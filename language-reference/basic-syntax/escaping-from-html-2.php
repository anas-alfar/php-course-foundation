<?php if ($loggedInUser) { ?>
<p>PHP engine parsed this line.</p>
<p>PHP engine parsed this line.</p>
<p>PHP engine parsed this line.</p>
<p>PHP engine parsed this line.</p>
<p>PHP engine parsed this line.</p>
<p>PHP engine parsed this line.</p>
<p>PHP engine parsed this line.</p>
<?php } else { ?>
<p>This line will be ignored by PHP engine and will be sent directly to
	the browser.</p>
<?php } ?>